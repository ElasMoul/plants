# Upload filter for `railway up` (read IN ADDITION to .gitignore). The image is
# runtime-only: it needs the CI-built JAR (at the snapshot ROOT — target/ is
# .gitignored and cannot be re-included across files) plus railway.json and
# Dockerfile.railway. Keep the snapshot lean.
.idea/
.vscode/
node_modules/
logs/
*.log
.env
src/
target/
