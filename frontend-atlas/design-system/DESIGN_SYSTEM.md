# Rhizome — design system

- **System:** Rhizome
- **Pinned version:** v1.0.0
- **Source registry ID:** `plantpal-cae942ee/v1.0.0` (design-studio design-system registry)

Cloned from design-studio's design-system registry; gaps are proposals, not local patches.

## Status of this copy

This directory is a **canonical, versioned pin — never a local fork.** The files here
are a byte-for-byte clone of the registry snapshot at v1.0.0.

- Do **not** hand-edit any file in this directory.
- If you find a gap, bug, or missing token/component, raise it as a **proposal back to
  the registry** (design-studio), then re-pin this directory to the new published
  version. A local patch would silently diverge this copy from the registry and break
  the pin.
- Version bumps happen by replacing the whole directory with the new snapshot and
  updating the pinned version above.
